# Admin credentials (used for seeding)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# Ingest key for incoming RTMP streams (optional, auto-generated if not set)
INGEST_KEY=

# Database path (for Docker volume)
DATABASE_PATH=/app/data/db.sqlite

# App URL (for auth callback)
NEXT_PUBLIC_APP_URL=https://live.dappvibe.com
