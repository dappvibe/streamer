services:
  app:
    build: .
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    ports:
      - "3000:3000"
      - "1935:1935"
      - "8080:8080"
    environment:
      - NODE_ENV=development
      - DATABASE_PATH=/app/data/db.sqlite
    command: npm run dev
